<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authActivity">
    <activity
        android:id="@+id/authActivity"
        android:name="com.shpp.ssierykh.assignment1.MainActivity"
        android:label="AuthActivity" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.shpp.ssierykh.assignment1.ProfileActivity"
        android:label="MainActivity" />
    <action
        android:id="@+id/action_global_mainActivity"
        app:destination="@id/mainActivity"
        app:enterAnim="@anim/slide_in_left"
        app:exitAnim="@anim/slide_in_right"
        app:popEnterAnim="@anim/slide_out_left"
        app:popExitAnim="@anim/slide_out_right" />
</navigation>