<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/authActivity">
    <activity
        android:id="@+id/authActivity"
        android:name="com.shpp.ssierykh.assignment1.ui.MainActivity"
        android:label="AuthActivity" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.shpp.ssierykh.assignment1.ui.MyProfileActivity"
        android:label="MainActivity" />
    <action
        android:id="@+id/action_global_mainActivity"
        app:destination="@id/mainActivity"
        app:enterAnim="@anim/slide_in"
        app:exitAnim="@anim/slide_out"
        app:popEnterAnim="@anim/fade_in"
        app:popExitAnim="@android:anim/fade_out" />
</navigation>