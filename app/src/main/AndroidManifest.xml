<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shpp.ssierykh.assignment1" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_kot_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment1"
        android:name=".utils.App">
        <activity
            android:name="com.shpp.ssierykh.assignment1.activity_and_splash.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.shpp.ssierykh.assignment1.activity_and_splash.SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>